# Archivo de ejemplo de variables de entorno para el backend
# Copie este archivo a .env y modifique según sea necesario

# Entorno de la aplicación
APP_ENV=development
APP_DEBUG=true

# Conexión a la base de datos
DB_CONNECTION=sqlite
DB_DATABASE=/var/www/html/database/database.sqlite

# JWT (JSON Web Token)
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRATION=86400  # 24 horas en segundos

# Google OAuth (necesario para la autenticación con Gmail)
# Estas credenciales deben ser obtenidas del Google Cloud Console
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost/auth/callback