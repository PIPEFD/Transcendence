# Monitoring endpoints for metrics
location /stub_status {
    stub_status;
    allow 127.0.0.1;
    deny all;
}

location /metrics {
    allow 127.0.0.1;
    deny all;
    proxy_pass http://nginx-exporter:9113/metrics;
}
